<Window x:Class="AiUsageBar.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AI Usage Bar" Height="620" Width="780" MinHeight="420" MinWidth="560"
        Background="{StaticResource SurfaceBrush}" Foreground="{StaticResource TextBrush}">
  <Grid Margin="20">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <DockPanel Margin="0,0,0,16">
      <Button x:Name="RefreshButton" DockPanel.Dock="Right" Content="Refresh" Padding="14,6" Click="Refresh_Click" />
      <TextBlock Text="AI Usage Bar" FontSize="24" FontWeight="SemiBold" VerticalAlignment="Center" />
    </DockPanel>
    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="220" />
        <ColumnDefinition Width="16" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <ListBox x:Name="Providers" DisplayMemberPath="DisplayName" SelectionChanged="Providers_SelectionChanged"
               Background="{StaticResource PanelBrush}" Foreground="{StaticResource TextBrush}" BorderThickness="0" />
      <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto" Background="{StaticResource PanelBrush}">
        <StackPanel x:Name="Details" Margin="16" />
      </ScrollViewer>
    </Grid>
    <TextBlock x:Name="Status" Grid.Row="2" Margin="0,16,0,0" Foreground="#A0A0A0" TextWrapping="Wrap" />
  </Grid>
</Window>
